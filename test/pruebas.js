init 

    vars 
    
        Num num = 10 
        String txt = 'El numero 1 es: ' 

    body
     
        log(txt, num) 
        if (num > 5) {
            log(numMayor5)
        }
        rpt {
            log(forExample)
        }


end